# Session Report — 2026-02-24 22:07

**Duration:** ~9 hours (13:25 → 22:07)
**Focus:** Giorgi Tax Calculation Forensic Fix (Phase 5 T5-S3)

---

## Objective
Fix the Giorgi tax calculation so the RAG pipeline correctly retrieves Article 82 (income tax exemptions for mountain residents) and Article 81 (20% income tax rate) to calculate exact tax amounts.

## What Was Done

### 1. Root Cause Analysis (Triple Failure Cascade)
- **Vector search crash:** `legacy_archived` filter field not in Atlas index → all `$vectorSearch` queries fail
- **BM25 fallback bias:** InfoHub documents dominating results, pushing out tax code articles
- **Auditor bypass:** Single-intent queries not checked for missing essential articles

### 2. Decomposer Prompt Fix (query_rewriter.py)
- Added Rule 5: Income tax queries always generate "Income Tax Exemptions" sub-query
- Added Rule 6: Demographic factors (mountain, children, age, disability) force exemption sub-query

### 3. Auditor Prompt Fix (query_rewriter.py)
- Added Rule 8: Calculation queries require precise numerical parameters (20%, 5%, 3000 GEL)
- Added Rule 9: Exemption sub-queries must find matsne article (Art. 82), not just InfoHub

### 4. Vector Search Architecture Fix (vector_search.py)
- Moved `legacy_archived` from `$vectorSearch` pre-filter to `$match` post-filter stage
- Pre-filter only supports Atlas-indexed fields; post-filter handles the rest
- 284 archived docs correctly excluded without crashing search

### 5. Test Infrastructure Fix (test_phase5_stress.py)
- Added `db_manager.connect()` — live test was running without DB connection
- Fixed settings import: `from config import settings` (not `from app.config`)

## Verification Results
- **102/102 unit tests pass** — zero regressions
- **Live pipeline test:** Art. 82 retrieved (score 0.86+), Art. 81 retrieved, sources correctly cited
- **LLM response quality:** Correctly distinguishes private sector vs budget org exemptions

## What Was NOT Completed
- T5-S3 test query needs exact Giorgi payload (annual 5000 GEL, not monthly)
- Full test with car sale + gift components not yet run
- Changes not committed or pushed

## Key Decisions
- `legacy_archived` stays as post-filter (no Atlas index change needed)
- Test query must use user's exact case text for deterministic results
- Next session starts with updating T5-S3 query and running final live test

## Modified Files
| File | Changes |
|---|---|
| `app/services/query_rewriter.py` | Decomposer rules 5-6, Auditor rules 8-9 |
| `app/services/vector_search.py` | Pre-filter → post-filter for legacy_archived |
| `tests/test_phase5_stress.py` | DB connection, query, assertions |
| `tests/test_vector_search.py` | 4 filter assertion updates |

## Open Questions
- Gift tax from uncle (160,000 GEL): Art. 82 exempts first 1,000? Or different threshold? LLM should calculate.
- Car tax (4 months ownership): 5% on gain per Art. 202? LLM should calculate.

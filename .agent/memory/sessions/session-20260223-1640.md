# Session Report — 2026-02-23 16:40–17:53

**Objective:** Fix expired SM retrieval leak + 422 error

---

## Completed Tasks

### 1. Frontend 422 Fix
- **Root Cause:** `FrontendChatRequest.message` had `max_length=500`, Georgian text exceeded it
- **Fix:** Increased to `max_length=5000` in `frontend_compat.py`
- **Debug:** Added temporary `RequestValidationError` handler to `main.py` (removed after fix)

### 2. Expired SM Structured Detection (Sprint 3)
- **Root Cause:** `detect_expired_sm()` only scanned title/body text; "ძალადაკარგული" exists in structured `status` object (`id=2`, `type="Powerless"`)
- **Debug:** Wrote Python script to fetch raw JSON for N 0106 → discovered `status.id == 2`
- **Fix:** Two-tier detection in `infohub_scraper.py` — Tier 1: structured metadata, Tier 2: keyword fallback
- **Result:** 218 expired SMs detected (was 1), 422 repealed chunks in MongoDB

### 3. Retrieval Leak Fix (3 Bypass Paths)
- `vector_search.py` — Added `$match: {"status": "active"}` to keyword search pipeline
- `tax_article.py` — Added `"status": "active"` to `find_by_number()` and `find_by_numbers()`

### 4. Deployment
- Backend committed (`75063de`) and pushed to `tax-agent-backend-1`
- Deployed to Cloud Run revision `tax-agent-backend-00018-j22`, healthy in 6.02s

---

## Modified Files

| File | Change |
|---|---|
| `app/api/frontend_compat.py` | `max_length` 500→5000 |
| `app/services/infohub_scraper.py` | Two-tier `detect_expired_sm()` |
| `app/services/vector_search.py` | `$match` status filter in keyword search |
| `app/models/tax_article.py` | Status filter in `find_by_number`/`find_by_numbers` |
| `tests/test_infohub_scraper.py` | 5 new tests (Tier 1 + N 0106 regression) |

---

## Test Results
- **45/45** infohub scraper tests passed
- **36/36** vector search tests passed

## Open Questions
- None — all fixes verified and deployed.

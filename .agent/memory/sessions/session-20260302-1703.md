# Session Log — 2026-03-02 17:03-17:51

**Focus:** Phase 1 Thinking Level Optimization — Implementation + Rollback

---

## Activities

### 1. Phase 1 Implementation (Blueprint → Code)
- Applied `thinking_level="minimal"` to 4 call sites in `query_rewriter.py`
- Migrated `critic.py` from raw dict config → `build_generation_config` + `thinking_level="minimal"`
- Added `thinking_enabled=True, thinking_level="minimal"` to `math_verifier.py`
- Implemented dynamic thinking routing in `rag_pipeline.py`:
  - `high` for: is_red_zone, multi-intent (sub_queries>1), INDIVIDUAL_INCOME, CORPORATE_TAX
  - `low` for: everything else (FAQ, GENERAL, single intent)
  - Applied to both streaming (L1827) and sync (L1208) paths
  - Added trajectory logging: `_trajectory["phases"]["generation_thinking"]`

### 2. E2E Validation v1
- **Simple FAQ** ("რა არის დღგ?"): E2E dropped 37.7s → **24.0s** (-36%)
  - Phase 0: 3188ms → 1862ms (-42%)
  - Thinking tokens: 1669 → 756 (-55%)
  - Generation: 21.7s → 17.4s (-20%)
  - Critic: 2.6s → 1.4s (-44%)
- **Complex Giorgi**: E2E = 57.1s (MV triggered: 4.2s with minimal)

### 3. Accuracy Regression Found
- `minimal` thinking on critic + math_verifier caused hallucinated 3,000 GEL exemption
- Critic failed to catch the error (insufficient reasoning at `minimal`)

### 4. Strategic Rollback
- `math_verifier.py`: minimal → **high**
- `critic.py`: minimal → **medium**
- `query_rewriter.py`: minimal → **low**
- `rag_pipeline.py`: **unchanged** (dynamic low/high kept)

### 5. E2E Validation v2 (Post-Rollback)
- **Complex Giorgi**: E2E = **86.5s** (WORSE than baseline!)
- Root cause: MV originally had `thinking_enabled=False` → now `True,high` = 28.1s
- Generation: 66.9s (think=3712 tokens — model non-determinism)
- Answer: salary tax = 10,800 GEL (highland exemption applied), total = 12,800 GEL

---

## Modified Files (UNCOMMITTED)
- `backend/tax_agent/app/services/query_rewriter.py` — 4 sites: thinking_level="low"
- `backend/tax_agent/app/services/critic.py` — build_generation_config + thinking_level="medium"
- `backend/tax_agent/app/services/math_verifier.py` — thinking_enabled=True, thinking_level="high"
- `backend/tax_agent/app/services/rag_pipeline.py` — dynamic low/high generation routing

## Decisions Made
- `minimal` is TOO restrictive for safety-critical agents (critic, MV)
- `low` is acceptable for classification (query_rewriter)
- Dynamic routing for generation is correct — keep it
- MV with `thinking_enabled=True,high` is too slow (28s) — needs reconsideration

## Open Questions
1. Should MV revert to `thinking_enabled=False` (original state)?
2. Is 10,800 GEL salary tax correct per Georgian tax code?
3. What's the right critic thinking level — `medium` adds 1.4s but catches hallucinations?

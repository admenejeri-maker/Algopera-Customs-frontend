# Session Report — 2026-03-01 21:18–23:27

## Objective
Fix Giorgi Turn 2 non-deterministic failure — multi-turn context contamination causing wrong tax calculations.

## Modified Files

### Backend (`tax_agent/`)
| File | Change |
|------|--------|
| `app/services/rag_pipeline.py` | Route on RAW query; skip rewriter for >150 chars; math verifier before critic; SSE heartbeat; history sliding window |
| `app/services/router.py` | +8 Georgian morphological keyword variants |
| `config.py` | `router_enabled=True`, `max_history_chars=4000` |
| `tests/test_giorgi_turn2_fixes.py` | 19 unit tests for all fixes (NEW) |

## Decisions Made

1. **Skip rewriter for long queries** (>150 chars) — rewriter was destructively compressing 590-char Giorgi query to 28-char nonsense, breaking vector search.
2. **Route on raw query, not rewritten** — rewriter stripped all domain keywords.
3. **Enable keyword router by default** — was `False`, so all classification fell to unified LLM fallback (`GENERAL`).
4. **MAX_CONTEXT_CHARS → 35000** on Cloud Run — was 10000, too small for multi-turn + full tax chunks.

## Deployment
- Commit `cf7c301` pushed to `origin/main`
- Cloud Run revision `00059-ms5` deployed, health check 200 OK

## Open Questions
1. Math verifier JSON parsing — circuit breaker still trips (prose wrapper around JSON)
2. Critic JSON parsing — `"Here is the JSON requested:"` prefix
3. Production E2E validation pending (Giorgi scenario on live URL)

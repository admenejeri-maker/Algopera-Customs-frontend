# Session Report — 2026-02-19 16:00-16:41

## Summary
QA adversarial review (Move 4) of 3-Layer Defense changes, followed by diagnosis and fix of Markdown formatting issue.

## Key Discovery
**Root cause of plain text responses:** Few-shot ✅ example in `BASE_SYSTEM_PROMPT` was itself plain text. Gemini copies the example format over rule text. Fixed with hybrid approach (prompt + frontend).

## Changes Made

### Backend
- `tax_system_prompt.py` — Few-shot ✅ rewritten with bold + bullets
- `test_system_prompt.py` — +1 test (`test_few_shot_uses_markdown_formatting`)
- `rag_pipeline.py` — +2 debug log statements (`system_prompt_built`, `generation_success`)

### Frontend
- `response-parser.ts` — New `ensureMarkdownFormatting()` function
- `chat-response.tsx` — Pipeline: raw → strip → format → parseProducts → render

## Test Results
- 29/29 system prompt tests ✅
- 366/373 full backend tests (7 pre-existing failures)
- Browser: bold first sentence confirmed ✅

## Decisions Made
1. Hybrid approach over pure prompt: Gemini unreliably follows formatting rules
2. `ensureMarkdownFormatting` only bolds first sentence (minimal, low-risk)
3. Bullet point formatting deferred — model doesn't use them even with updated few-shot

## Open Questions
1. Should debug logs be removed before deploying?
2. Should `stripSourcesFooter` get unit tests? (QA finding F1)
3. More aggressive bullet formatting needed?

## Status
- **NOT committed** — all changes are local
- **NOT deployed** — Cloud Run still has previous version
